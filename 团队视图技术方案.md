# 团队视图技术方案

> 依据低保真原型 [value.html](value.html)「团队视图主体」模块整理。目标：把原型里的静态示意数据，替换为真实的 Lock Bot + Monquery + 组织架构数据，并落地到 `web/` 生产应用中。
> 本文档只覆盖**团队视图**，不涉及集群视图、个人视图的既有实现（可直接复用其数据口径和基础设施）。

---

## 1. 原型模块清单（value.html 对应关系）

| 原型区块 | 说明 | 数据性质 |
| --- | --- | --- |
| 团队视图主体 · 筛选条 | 选时间范围（最近 1/2/7/30/90 天、6 个月、自定义），可选至当前时间 | 动态历史（截至最新可对齐采样点） |
| 团队效率（默认收起） | 团队总卡时 / 算力利用率排行表 + 团队总卡时占比饼图 | 所选范围内动态计算，多团队横向对比 |
| 选择团队 + 实时快照卡片 | 当前团队锁定节点数 / 锁定卡数 | 实时（Lock Bot 当前状态） |
| 团队节点使用率趋势 | 近 N 天团队已锁定卡数占全集群 448 卡的比例折线 | 动态历史轨迹（可包含当天） |
| 团队 XPU / 显存利用率趋势 | 近 N 天团队算力、显存利用率折线 | 按锁定归因的采样点算术平均（可包含当天） |

实时快照卡片展示当前 Lock Bot 状态；效率表和趋势图按所选范围重算，最晚展示到 Lock Bot 锁定信息与 Monquery 指标均可对齐的最新采样点（dataAsOf）。两者的时间点不同，页面必须分别标明：快照显示 asOf，统计与趋势显示 dataAsOf。不再强制统计到昨日 24:00；当天数据会随刷新和上游记录补齐而更新。

---

## 2. 技术栈与框架选型

与 `web/` 现有生产应用保持一致，**不引入新框架**：

- 前端：Vue 3 + Vite（复用现有 `web/src/` 目录结构：`views/`、`services/`）。
- 后端：Node.js 原生 `http`（`web/server/proxy.cjs` 路由层），不引入 Express/Koa 等框架，保持和 `trend-service.cjs` / `trend-store.cjs` 一致的裸 Node 风格。
- 数据存储：`node:sqlite`（实验性 API，Node 22+），复用 `trend-store.cjs` 的模式，用于团队维度的历史聚合缓存。
- 测试：`node --test`，用例写在 `web/test/`，遵循 CLAUDE.md 里"新增聚合/缓存相关改动需要补测试"的要求。

理由：团队视图本质是集群趋势的一个"按用户分桶再聚合"的变体，数据源（Lock Bot、Monquery）和时区/槽位处理（`china-time.js`）完全复用，没有必要为此引入新技术栈。

---

## 3. 数据来源

| 数据 | 来源 | 现状 |
| --- | --- | --- |
| 节点/卡实时锁定状态 | Lock Bot `GET /api/bots/{id}/state` | 已接入（`web/src/services/cluster-state.js`） |
| 历史占用记录 | Lock Bot `GET /api/bots/{id}/occupancy?date=` | 已接入（`web/server/trend-service.cjs`） |
| XPU / 显存利用率 | Monquery `getHistoryitemdata`（节点级 XPU、单卡 XPU/显存） | 已接入；团队聚合需保留节点/卡维度 |
| **user_id → 所属团队** | **组织架构接口（内部 HR/组织系统）** | **未接入，本方案新增** |

### 3.1 待你确认的接口细节（阻塞项）

组织架构接口的具体信息我目前拿不到，需要你提供，否则 §5 的服务端实现无法写死：

1. 接口地址（host/port 或内网域名）、鉴权方式（是否复用 Lock Bot 的 Bearer Token，还是独立的账号/密钥）。
2. 查询粒度：能否批量查询"某一批 user_id 对应的团队"，还是只能按团队反查成员列表（后者更适合我们，因为团队数量远小于用户数量）。
3. 返回的团队标识：是团队名字符串，还是团队 ID（需要额外一张团队 ID → 团队名映射表）。
4. 数据更新频率：团队结构多久变化一次，是否需要考虑"历史某天的锁定记录，应该按当时的团队归属计算，还是按当前团队归属计算"这种口径问题（人员转岗场景）。

在拿到答复前，本文档第 5 节用一个可替换的抽象接口 `resolveTeamMembers()` 占位，先把上下游数据流和计算口径定下来，接口细节确定后只需替换这一层实现。

---

## 4. 数据模型定义

### 4.1 团队成员映射（新增，`web/shared/` 或 server 内部缓存）

```ts
// 团队 → 成员 user_id 列表；从组织架构接口拉取后缓存
interface TeamRoster {
  [teamName: string]: string[]; // user_id 列表
}
```

- 参照 `web/shared/cluster-scope.json` 的"唯一权威来源"原则：团队成员映射也应该有唯一来源，**不允许**在前端和后端各存一份、或者散落在多个组件里硬编码。
- 缓存策略：组织架构数据变化频率低，建议服务端按天缓存（结构上可直接照抄 `trend-service.cjs` 里 `LOCK_HISTORY_CACHE_*` 的日缓存模式），启动时预热 + 每日定时刷新，避免每次请求都打组织架构接口。

### 4.2 团队实时锁定快照（对应"选择团队"下方的统计卡片）

```ts
interface TeamLiveSnapshot {
  team: string;
  lockedNodeCount: number;   // 该团队成员持有 NODE/QUEUE 整机锁的节点数
  lockedCardCount: number;   // lockedNodeCount * CARD_COUNT + 该团队独立 DEVICE 锁覆盖的卡数
  asOf: number;              // 快照时间（Unix 秒），每 5 分钟刷新
}
```

计算方式**直接复用** `cluster-state.js` 的 `mergeLockBotStates()` 结果，只是多一层按 `current_users[].user_id → team` 的过滤：

1. 用 `mergeLockBotStates()` 得到全集群的卡级锁定状态（`deviceState`）。
2. 对每个节点的每张卡，若其 `current_users` 中任意一个 `user_id` 属于目标团队，则该卡计入该团队。
3. `lockedNodeCount` = 该团队"整节点被本团队成员通过 NODE/QUEUE 锁独占"的节点数（8 卡全部计入该团队且锁类型是整机锁）。
4. `lockedCardCount` = `lockedNodeCount × CARD_COUNT` + 团队通过 DEVICE 锁额外覆盖、但所在节点未被计入 `lockedNodeCount` 的卡数。

这与原型公式 `8 × 8 + 5 = 69`（8 个整机锁节点 × 8 卡 + 5 张单独 DEVICE 锁的卡）完全对应。

> 边界情况：同一节点上，团队 A 和团队 B 的成员分别锁了不同卡（DEVICE 粒度共享节点）。此时该节点不计入任何团队的 `lockedNodeCount`，只按卡计入各自的 `lockedCardCount`，这一点和 CLAUDE.md 里"DEVICE 锁仅覆盖 device_id 指定的卡"的既有规则一致。

### 4.3 团队效率（对应“团队效率”表 + 饼图）

接口字段：`team`、`totalCardHours`、`xpuUtilization`、`memUtilization`、`cardHourShare`、`dataAsOf`。`totalCardHours` 是所选范围内累计卡时，结束于 `dataAsOf`；XPU 与显存利用率均为所选范围内的采样点算术平均；`dataAsOf` 是 Lock Bot 与 Monquery 均可对齐的最新采样点。

**计算口径**：

- **团队总卡时**：给定时间范围内，将该团队成员的 Lock Bot occupancy 记录与当天仍在进行的 state 锁合并；以节点:卡去重、并裁剪至 [start, dataAsOf] 后求和。NODE/QUEUE 锁按 8 卡计，DEVICE 锁按实际 dev_id 计；重叠记录仅计一次。数据来自每个节点的占用详情，最后按 user_id 到 team 的映射分桶求和。
- **团队节点使用率（趋势）**：每个采样时刻以团队已锁定卡数占全集群固定总卡数的比例表示，公式为 `nodeUtilization = 团队已锁定卡数 ÷ (56 × CARD_COUNT) × 100`。分子按 `node:card` 去重：NODE/QUEUE 整机锁覆盖该节点 8 卡，DEVICE 锁仅覆盖指定卡，同卡重叠锁只计一次；分母固定为 56 个计算节点 × 8 卡 = 448 卡，与集群视图“节点使用率”口径一致。Lock Bot 数据缺失时该点返回 `null`，不以 0 代替。
- **采样点上的锁定资源**：每个采样时刻 t，先根据锁记录确定资源归属的团队。NODE/QUEUE 整机锁将整个节点作为一个锁定资源；DEVICE 锁将指定卡作为一个锁定资源。NODE/QUEUE 锁覆盖的节点不再把其中重叠的 DEVICE 锁重复计入。
- **算力 / 显存利用率（算术平均）**：
  - NODE/QUEUE 锁取该节点的 XPU_AVERAGE_UTILIZATION；即使用户只使用一张卡，其余卡空闲而拉低节点平均利用率，也完整归因给该用户所属团队，作为资源使用效率的一部分。节点的显存利用率取该时刻 8 张卡 XPUn_MEM_UTILIZATION 的算术平均。
  - DEVICE 锁取对应卡的 XPUn_XPU_UTILIZATION 与 XPUn_MEM_UTILIZATION。
  - 团队在时刻 t 的任一指标 = 该团队当时所有锁定资源的利用率之和 ÷ 该团队当时全部锁定资源数。若任一锁定资源缺少相应 Monquery 指标，该指标点返回 null，不以 0 补齐，也不从分母剔除；指标值本身为 0 时是有效数据。
  - 效率表的范围利用率等于所选范围内全部有效采样点的团队利用率算术平均；趋势图直接绘制每个采样点的团队利用率。固定采样间隔下，这与集群视图每个采样点先算平均、再对时序求平均的口径一致。
- **采样边界补偿**：若当前采样点某资源没有锁记录但利用率非 0，则仅查看该资源的前一个采样点；前一采样点有锁时，将当前点归因给前一采样点的同一团队。该补偿最多跨一个采样间隔；前一点无锁或无法确认团队时不计入任何团队。
- **团队总卡时占比（饼图）** = 该团队总卡时 ÷ 所有团队总卡时之和。原型标题统一使用团队总卡时占比，不再使用容易与实时节点数混淆的团队节点使用率占比。
- **排序**：效率表默认按算力利用率降序排列；点击表头可切换为按总卡时排序，两个方向都要支持。迁移到 Vue 组件后使用 sortKey/sortAsc 的响应式状态，不需要手写 DOM 操作。

### 4.4 团队趋势（对应三张折线图）

每个趋势点包含 sampledAt、nodeUtilization、xpuUtilization、memUtilization。nodeUtilization 是该团队已锁定卡数 ÷（56 个计算节点 × 8 卡）× 100；xpuUtilization 与 memUtilization 分别是该采样点的团队 XPU 与显存利用率算术平均。

- 采样间隔对齐现有集群趋势策略，团队视图直接复用同一套映射。
- nodeUtilization 的历史轨迹来自 Lock Bot 历史状态快照序列；如果只有 occupancy 记录和当前状态，则用占用区间反推每个采样点该团队锁定的节点:卡。NODE/QUEUE 按 8 卡、DEVICE 按实际卡数计入，同一节点:卡仅计一次。
- 最后一个点可以是当天的最新 dataAsOf，不需要等待昨日 24:00；页面显示该时间，并在刷新后替换当天未稳定的统计结果。
- 缺数据时不伪装为 0，返回 null，前端按空段处理，不连线。

---

## 5. 后端实现方案

### 5.1 新增模块

```
web/server/
  org-service.cjs      # 新增：封装组织架构接口，导出 resolveTeamRoster()，内置日缓存
  team-trend-service.cjs  # 新增：team 粒度的聚合，内部复用 trend-service.cjs 已有的 Lock Bot 拉取 + Monquery 拉取逻辑
```

`org-service.cjs` 对外接口（占位，等你给接口细节后再实现内部请求逻辑）：

```js
// 返回 { [teamName]: string[] userIds }，内部按天缓存
async function resolveTeamRoster(config) { ... }
```

`team-trend-service.cjs` 的核心思路是复用已有的 Lock Bot、Monquery 拉取与缓存能力，但在聚合前保留资源粒度：

1. 复用 trend-service.cjs 的请求、时区、锁记录和缓存基础能力，但把 Monquery 解析上提为按节点到采样点、节点:卡到采样点保留的原始序列；不能复用其已经汇总为全集群均值的最终数组。
2. 扩展 Monquery 请求项：NODE/QUEUE 所需的节点 XPU_AVERAGE_UTILIZATION，以及 DEVICE 所需的每卡 XPUn_XPU_UTILIZATION、XPUn_MEM_UTILIZATION。
3. 让 occupancyIntervals() / stateIntervals() 保留 user_id、锁类型和覆盖卡；用 resolveTeamRoster() 映射为 team，再按每个采样点构造团队锁定资源。
4. 按第 4.3 节的 NODE/QUEUE 与 DEVICE 规则计算每个团队的节点使用率、XPU/显存点值、卡时和 dataAsOf；当前日期的 state 数据在每次请求时重新读取，不等待日结。

### 5.2 新增路由

在 `web/server/proxy.cjs` 里新增（仿照 `/api/cluster-trend` 的参数校验风格）：

```
GET /api/team-list
  → 返回团队名列表（供前端"选择团队"下拉框、效率表渲染）

GET /api/team-snapshot?team=xxx
  → 返回 TeamLiveSnapshot（实时锁定节点/卡数）

GET /api/team-efficiency?start=&end=
  → 返回全部团队的 TeamEfficiency[] 及 dataAsOf（效率表 + 饼图共用同一个响应）

GET /api/team-trend?team=xxx&start=&end=&interval=
  → 返回该团队的 TeamTrendPoint[] 及 dataAsOf（三张趋势图共用同一个响应）
```

参数校验规则对齐 `/api/cluster-trend` 现有实现：时间范围最长 6 个自然月、`interval` 限定在既有枚举集合内、鉴权透传浏览器 `Authorization` 到 Lock Bot（**不落盘、不写日志**，遵守 CLAUDE.md 的安全约束）。

---

## 6. 前端实现方案

新增 `web/src/views/TeamDashboard.vue`（或在现有 `ClusterDashboard.vue` 旁新增一个视图，具体路由方式取决于你们现在个人/集群视图是怎么切换的，需要你确认是否要复用同一套 tab 切换组件）。

业务逻辑继续放在 `web/src/services/` 下，新增：

- `team-api.js`：对上面 4 个新接口的 fetch 封装，模式抄 `api.js`。
- `team-state.js`：团队选择、筛选时间范围的响应式状态管理。
- 图表渲染继续复用 `chart-data.js` 里现成的折线图数据转换逻辑（如果它当前是集群专用的，需要抽出通用部分）。

组件拆分建议对应原型里的模块划分：

| Vue 组件 | 对应原型区块 |
| --- | --- |
| `TeamRangeFilter.vue` | 筛选条（快捷范围 + 自定义区间） |
| `TeamEfficiencyPanel.vue` | 团队效率表 + 饼图（含展开/收起交互） |
| `TeamLiveSnapshot.vue` | 实时锁定节点/卡数统计卡片 |
| `TeamTrendCharts.vue` | 三张趋势折线图 |

展开/收起交互直接用一个 `ref(false)` 控制 `v-if`/`v-show`，不需要像原型 HTML 里那样手写 `hidden` 属性和事件监听。

---

## 7. 测试计划

按 CLAUDE.md 要求，新增聚合逻辑必须补充 `web/test/` 用例，建议新建 `team-data.test.mjs`，至少覆盖：

1. resolveTeamRoster() 缓存命中/未命中、组织接口失败时的降级行为。
2. 团队总卡时的节点:卡去重逻辑：同一用户 NODE 锁 + DEVICE 锁重叠时不重复计数。
3. 团队节点使用率按去重后的已锁定 `node:card` ÷ 448 卡计算：NODE/QUEUE 锁覆盖 8 卡，DEVICE 锁仅计指定卡，重叠锁不得重复计数，Lock Bot 数据缺失时返回 null。
4. NODE/QUEUE 锁按节点指标、DEVICE 锁按单卡指标计算；整机锁只运行一张卡时，节点低利用率仍完整归因给持锁团队。
5. 每个采样点先按团队锁定资源求算术平均，效率表再对有效采样点求算术平均；指标为 0 是有效值，监控缺失不进入分母。
6. 实时快照的整机锁 vs 单卡锁边界情况：同节点被两个团队分卡持有时，两个团队的 lockedNodeCount 都不应计入该节点。
7. 采样点无锁但前一采样点有锁时仅补偿一个间隔；无可归因锁或监控数据不完整时返回 null 而非 0。
8. 当天数据截至最新可对齐 dataAsOf，不会被截断至昨日 24:00。

---

## 8. 尚未解决、需要你决策的问题

1. **组织架构接口细节**（§3.1）——这是当前最大的阻塞项，没有这个就无法把"用户 → 团队"这一层从占位函数变成真实实现。
2. **人员转岗的历史口径**：如果 6 月的锁定记录，人员当时在团队 A，现在转到团队 B 了，团队效率表和趋势图该按"当时"还是"现在"的团队归属统计？这直接决定 `resolveTeamRoster()` 要不要支持"按日期查历史归属"。
3. **视图切换方式**：团队视图和现有集群视图/个人视图是同一个 Vue 应用里的 tab（像原型 `data-view-tab` 那样），还是独立路由/独立页面？这决定 §6 的组件挂载方式。

建议先拿到第 1 点的答复，其余三点可以在开发过程中逐步确认，不阻塞后端骨架搭建。
